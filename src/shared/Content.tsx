import React from 'react'
import styled from 'styled-components/native'

const Content = (props: any) => <ContentStyle {...props} />

const ContentStyle = styled.View`
  margin-bottom: 10
  line-height: 22
`

export default Content
